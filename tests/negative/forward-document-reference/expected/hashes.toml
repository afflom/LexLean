# Platform-independent artifact hashes (compared exactly) and the
# process-bound files whose bytes vary per host, toolchain build, and
# compiler executable (presence only). SPEC.md §28.2, §28.4.

[platform_independent]

[process_bound]
files = []
