\begin{lexlean}{Main}
\title{Atlas definition two label}

\begin{section}{header}
\heading{Atlas theorem one label}
  \begin{theorem}{dimension-is-fixed}
  \noaxioms
  For every natural number \(n\), \(n + 0 = n\).
  \begin{proof}
  Close the goal by reflexivity.
  \end{proof}
  \end{theorem}
\end{section}

\begin{section}{roots}
\heading{Atlas theorem five label}
  \begin{theorem}{add-successor}
  \noaxioms
  For every natural number \(x\) and natural number \(y\), \(x + (y + 1) = x + y + 1\).
  \begin{proof}
  Close the goal by reflexivity.
  \end{proof}
  \end{theorem}

  \begin{theorem}{root-count-is-a-constant}
  \noaxioms
  For every natural number \(n\), \(0 + n = n\).
  \begin{proof}
  \begin{induction}{n}
  \begin{case}{lexlean.std.nat::zero}
  \bind{}
  Close the goal by reflexivity.
  \end{case}
  \begin{case}{lexlean.std.nat::succ}
  \bind{m;ih}
  \begin{rewrite}{goal}
  \forward{\reference{Main::add-successor}(0, m)}
  \forward{ih}
  \end{rewrite}
  \end{case}
  \end{induction}
  \end{proof}
  \end{theorem}
\end{section}
\end{lexlean}
