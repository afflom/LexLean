spec = "lexlean/entry/1"
id = "succ-add"
category = "proof-constant"
signature = "(pi ((explicit n (const lexlean.std.nat::nat)) (explicit m (const lexlean.std.nat::nat))) (app (const lexlean.core::eq) (app (const lexlean.std.nat::add) (app (const lexlean.std.nat::succ) (local n)) (local m)) (app (const lexlean.std.nat::succ) (app (const lexlean.std.nat::add) (local n) (local m)))))"
surface_arity = 2
frame = "call"

[denotation]
kind = "lean"
module = "Init"
name = "Nat.succ_add"

[[form]]
id = "succ-add"
channel = "math"
surface = "succadd"
canonical_source = true
features = []

[render]
math = "(seq (operator-name succadd) (paren (seq (slot 0) (token comma) (space) (slot 1))))"
