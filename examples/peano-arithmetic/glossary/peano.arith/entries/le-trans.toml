spec = "lexlean/entry/1"
id = "le-trans"
category = "proof-constant"
signature = "(pi ((implicit n (const lexlean.std.nat::nat)) (implicit m (const lexlean.std.nat::nat)) (implicit k (const lexlean.std.nat::nat)) (explicit h1 (app (const lexlean.std.nat::le) (local n) (local m))) (explicit h2 (app (const lexlean.std.nat::le) (local m) (local k)))) (app (const lexlean.std.nat::le) (local n) (local k)))"
surface_arity = 2
frame = "call"

[denotation]
kind = "lean"
module = "Init"
name = "Nat.le_trans"

[[form]]
id = "le-trans"
channel = "math"
surface = "letrans"
canonical_source = true
features = []

[render]
math = "(seq (operator-name letrans) (paren (seq (slot 0) (token comma) (space) (slot 1))))"
