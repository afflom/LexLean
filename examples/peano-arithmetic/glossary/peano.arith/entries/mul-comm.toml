spec = "lexlean/entry/1"
id = "mul-comm"
category = "proof-constant"
signature = "(pi ((explicit n (const lexlean.std.nat::nat)) (explicit m (const lexlean.std.nat::nat))) (app (const lexlean.core::eq) (app (const lexlean.std.nat::mul) (local n) (local m)) (app (const lexlean.std.nat::mul) (local m) (local n))))"
surface_arity = 2
frame = "call"

[denotation]
kind = "lean"
module = "Init"
name = "Nat.mul_comm"

[[form]]
id = "mul-comm"
channel = "math"
surface = "mulcomm"
canonical_source = true
features = []

[render]
math = "(seq (operator-name mulcomm) (paren (seq (slot 0) (token comma) (space) (slot 1))))"
