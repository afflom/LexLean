spec = "lexlean/entry/1"
id = "mul-assoc"
category = "proof-constant"
signature = "(pi ((explicit n (const lexlean.std.nat::nat)) (explicit m (const lexlean.std.nat::nat)) (explicit k (const lexlean.std.nat::nat))) (app (const lexlean.core::eq) (app (const lexlean.std.nat::mul) (app (const lexlean.std.nat::mul) (local n) (local m)) (local k)) (app (const lexlean.std.nat::mul) (local n) (app (const lexlean.std.nat::mul) (local m) (local k)))))"
surface_arity = 3
frame = "call"

[denotation]
kind = "lean"
module = "Init"
name = "Nat.mul_assoc"

[[form]]
id = "mul-assoc"
channel = "math"
surface = "mulassoc"
canonical_source = true
features = []

[render]
math = "(seq (operator-name mulassoc) (paren (seq (slot 0) (token comma) (space) (slot 1) (token comma) (space) (slot 2))))"
