spec = "lexlean/entry/1"
id = "append"
category = "infix-function"
signature = "(pi ((implicit a (sort (type u))) (explicit xs (app (const example.list::list) (local a))) (explicit ys (app (const example.list::list) (local a)))) (app (const example.list::list) (local a)))"
universes = ["u"]
surface_arity = 2
frame = "infix"
precedence = 65
associativity = "right"

[denotation]
kind = "lean"
module = "Init"
name = "List.append"

[[form]]
id = "append"
channel = "math"
surface = "⧺"
canonical_source = true
features = []

[render]
math = "(seq (slot 0) (space) (token plus) (token plus) (space) (slot 1))"
