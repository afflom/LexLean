spec = "lexlean/entry/1"
id = "blt"
category = "function"
signature = "(pi ((explicit a (const lexlean.std.nat::nat)) (explicit b (const lexlean.std.nat::nat))) (const lexlean.std.bool::bool))"
surface_arity = 2
frame = "call"

[denotation]
kind = "lean"
module = "Init"
name = "Nat.blt"

[[form]]
id = "blt"
channel = "math"
surface = "blt"
canonical_source = true
features = []

[render]
math = "(seq (operator-name blt) (paren (seq (slot 0) (token comma) (space) (slot 1))))"
