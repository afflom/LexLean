spec = "lexlean/entry/1"
id = "ne"
category = "infix-predicate"
signature = "(pi ((explicit a (const lexlean.std.nat::nat)) (explicit b (const lexlean.std.nat::nat))) (sort prop))"
surface_arity = 2
frame = "infix"
precedence = 50
associativity = "none"

[denotation]
kind = "defined"
value = "(lam ((explicit a (const lexlean.std.nat::nat)) (explicit b (const lexlean.std.nat::nat))) (app (const lexlean.core::lnot) (app (const lexlean.core::eq) (local a) (local b))))"

[[form]]
id = "ne"
channel = "math"
surface = "≠"
canonical_source = true
features = []

[render]
math = "(seq (slot 0) (space) (token not-equals) (space) (slot 1))"
