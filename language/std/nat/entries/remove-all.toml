spec = "lexlean/entry/1"
id = "remove-all"
category = "function"
signature = "(pi ((explicit a (app (const lexlean.std.nat::list) (const lexlean.std.nat::nat))) (explicit b (app (const lexlean.std.nat::list) (const lexlean.std.nat::nat)))) (app (const lexlean.std.nat::list) (const lexlean.std.nat::nat)))"
surface_arity = 2
frame = "call"

[denotation]
kind = "lean"
module = "Init"
name = "List.removeAll"

[[form]]
id = "removeall"
channel = "math"
surface = "removeall"
canonical_source = true
features = []

[render]
math = "(seq (operator-name removeall) (paren (seq (slot 0) (token comma) (space) (slot 1))))"
