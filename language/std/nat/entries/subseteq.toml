spec = "lexlean/entry/1"
id = "subseteq"
category = "infix-predicate"
signature = "(pi ((explicit a (app (const lexlean.std.nat::list) (const lexlean.std.nat::nat))) (explicit b (app (const lexlean.std.nat::list) (const lexlean.std.nat::nat)))) (sort prop))"
surface_arity = 2
frame = "infix"
precedence = 50
associativity = "none"

[denotation]
kind = "lean"
module = "Init"
name = "List.Subset"

[[form]]
id = "subseteq"
channel = "math"
surface = "⊆"
canonical_source = true
features = []

[render]
math = "(seq (slot 0) (space) (token subset-equal) (space) (slot 1))"
